{
  "name": "@webby/root",
  "scripts": {
    "fmt": "prettier . --write",
    "dev": "turbo run dev",
    "build": "turbo run build"
  },
  "devDependencies": {
    "prettier": "^3.0.1",
    "turbo": "^1.10.12"
  },
  "prettier": {
    "singleQuote": true,
    "printWidth": 120
  }
}
