import { FC } from 'react';
import { Assignment } from '@/utils/types/wk-assignment';

export const ShowView: FC<{ assignment: Assignment }> = ({ assignment }) => {
  return <h1>It means something</h1>;
};
